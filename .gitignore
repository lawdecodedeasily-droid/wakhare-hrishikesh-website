.DS_Store
Thumbs.db
desktop.ini

# Raw client media (originals, unoptimised, never served) — optimised copies live in images/
Testimonial_s for Website(Hrishikesh)/

# Redesign planning docs — internal only, not for the public repo
REDESIGN-PROMPT.md
REDESIGN-BRIEF.md
OPEN-QUESTIONS.md

# Astro build output and deps
dist/
.astro/
node_modules/

# environment variables
.env
.env.production
